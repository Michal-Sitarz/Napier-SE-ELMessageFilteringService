<UserControl x:Class="ELMessageFilteringService.Views.DisplayMessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ELMessageFilteringService.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="700"
             Background="DarkOrange">
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="{Binding IdTextBlock}"/>
            <TextBox Grid.Column="1" Text="{Binding IdTextBox}"/>
            <TextBlock Grid.Column="0" Text="{Binding TypeTextBlock}"/>
            <TextBox Grid.Column="1" Text="{Binding TypeTextBox}"/>
            <TextBlock Grid.Column="0" Text="{Binding IdTextBlock}"/>
            <TextBox Grid.Column="1" Text="{Binding IdTextBox}"/>
        </Grid>
    </StackPanel>
</UserControl>
